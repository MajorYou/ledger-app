/**
 * 页面元数据（向后兼容 re-export）
 * 实际定义已迁移至 lib/ai-capabilities.ts
 */
export type { PageMetadata } from '@/lib/ai-capabilities'
export { allPageMetadata, ALLOWED_REDIRECT_PATHS } from '@/lib/ai-capabilities'
